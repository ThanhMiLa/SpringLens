<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>vn.io.codelearning.springapitester</id>
    <name>SpringLens</name>
    <vendor email="vntvlogs@gmail.com" url="https://github.com/ThanhMiLa/SpringLens">ThanhMiLa</vendor>

    <description><![CDATA[
        <p><b>Spring API Tester</b> là một plugin siêu nhẹ hỗ trợ kiểm thử API.</p>
        <p>Công cụ này được thiết kế để tự động nhận diện các endpoint của Spring Boot (@GetMapping, @PostMapping...) thông qua cây cú pháp PSI, giúp nhà phát triển backend kiểm thử trực tiếp trên IDE mà không cần chuyển đổi sang Postman.</p>
    ]]></description>

    <!-- Phụ thuộc nền tảng IntelliJ mặc định -->
    <depends>com.intellij.modules.platform</depends>
    <!-- Phụ thuộc module Java để lấy quyền phân tích cú pháp PSI của Java -->
    <depends>com.intellij.modules.java</depends>

    <!-- Tạm thời để trống. Chúng ta sẽ thêm class Java vào đây sau -->
    <extensions defaultExtensionNs="com.intellij">
    <toolWindow id="SpringLens"
                anchor="right"
                factoryClass="vn.io.codelearning.springapitester.ui.SpringLensToolWindowFactory"
                icon="AllIcons.Nodes.WebFolder"/>
    <projectService serviceImplementation="vn.io.codelearning.springapitester.state.SpringLensState"/>
  </extensions>
</idea-plugin>