package vn.io.codelearning.springapitester.model;

/**
 * Request detail tabs whose selection is remembered independently for each endpoint.
 */
public enum RequestTab {
    PARAMS,
    HEADERS,
    COOKIES,
    AUTH,
    BODY
}
